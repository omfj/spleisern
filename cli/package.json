{
  "name": "@spleis/cli",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cargo build --release",
    "install:system": "cp ../target/release/spleis-cli ~/.local/bin/spleis",
    "test": "cargo test",
    "format": "cargo fmt",
    "lint": "cargo clippy -- -D warnings",
    "lint:fix": "cargo clippy --fix --allow-dirty --allow-staged"
  }
}